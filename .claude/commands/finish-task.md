以下のサイクルをエージェント自身が繰り返す。(ユーザーに操作を依頼しない)

1. `pnpm lint && pnpm build && pnpm test` でテストをすべてパスさせる
2. カスタムサブエージェント `code-reviewer` にレビューさせる（`.claude/agents/code-reviewer.md` に定義)
3. レビュー内容が妥当なら修正し、1に戻る
4. レビューで問題なしならタスク完了
5. レビューは最大5回まで
